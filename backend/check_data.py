import weaviate; from app.services.structured_data_service import structured_data_service; client = weaviate.Client("http://weaviate:8080"); tenant_id = "9d8d5dcf-f6b3-4d06-8016-0f24869f8872"; print("Prüfe strukturierte Daten für Tenant:", tenant_id); for data_type in ["school", "office", "event"]: class_name = structured_data_service.get_class_name(tenant_id, data_type); print(f"Klasse: {class_name}"); try: result = client.query.aggregate(class_name).with_meta_count().do(); count = result["data"]["Aggregate"][class_name][0]["meta"]["count"]; print(f"{data_type}: {count} Objekte"); except Exception as e: print(f"{data_type}: Fehler - {str(e)}")
